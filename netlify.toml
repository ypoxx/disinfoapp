[build]
  base = "spin"
  command = "npm install && npm run build"
  publish = "spin/dist"

[build.environment]
  NODE_VERSION = "20"

# SPA redirect - all routes serve index.html
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
